# Índice de desconexão — call de setup

Mede quanto da call foi gasto **fora** do foco do setup [REDACTED_INSTRUCTION], de forma auditável.

Não é o índice de [VALIDATION_STAGE]: não existe `[REDACTED_INSTRUCTION]` aqui. O cruzamento é com `[REDACTED_INSTRUCTION]`.

## Antes de começar

Se faltar, pergunte:

1. Transcrição (com timestamp se houver)
2. Duração **real** da call em minutos (nunca a agendada)
3. Data `[REDACTED_INSTRUCTION]`
4. Caminho de `[REDACTED_INSTRUCTION]` [REDACTED_INSTRUCTION]
5. Caminho de `[REDACTED_INSTRUCTION]` (cria se não existir)

## Passo 1 — Varredura

```bash
[REDACTED_CODE_OR_CONFIG]
```

Categorias: [REDACTED_INSTRUCTION]

A lista é piso, não teto. Releia o primeiro e o último bloco da call mesmo sem match. Frases novas óbvias: trate como candidato na mesma hora.

## Passo 2 — Cruzar e julgar

```bash
[REDACTED_CODE_OR_CONFIG]
```

`[REDACTED_INSTRUCTION]` é pista léxica, não veredito.

- **CONFIRMADO** — [REDACTED_INSTRUCTION]
- **DESCARTADO** — [REDACTED_INSTRUCTION]

**[EXTERNAL_SYSTEM]:** [TASK_CODE] é setup (fornecer acesso). Discutir bug antigo do [EXTERNAL_SYSTEM] sem relação com o cadastro/acesso → desconexão. Testar se a [SYSTEM_LINK] "funciona" → [VALIDATION_STAGE] [REDACTED_INSTRUCTION].

**Encerramento antecipado:** call marcada [METRIC], durou [REDACTED_INSTRUCTION]

Categoria social **só** antes do primeiro conteúdo de setup (`[REDACTED_INSTRUCTION]`) e depois do último (`[REDACTED_INSTRUCTION]`). "Bom dia" no minuto 40 para quem atrasou não conta. Abertura típica 15s–[METRIC]; se passar de ~[METRIC], separe o que for negociação.

Confirme trechos assim:

```json
[REDACTED_CODE_OR_CONFIG]
[REDACTED_CODE_OR_CONFIG]
[REDACTED_CODE_OR_CONFIG]
[REDACTED_CODE_OR_CONFIG]
```

`[REDACTED_INSTRUCTION]` obrigatório e específico.

## Passo 3 — Somar (nunca de cabeça)

```bash
[REDACTED_CODE_OR_CONFIG]
```

`[REDACTED_INSTRUCTION]` não sobrescreve datas já gravadas. Correção do mesmo dia: edite o arquivo na mão.

Entregue a lista de confirmados + quantos candidatos o script achou vs. quantos você confirmou, junto com `[REDACTED_INSTRUCTION]`.

Sem timestamp: avise; âncora abertura em `[REDACTED_INSTRUCTION]` e encerramento até a duração real.

## Palavras-chave

A lista bundled está em `[REDACTED_INSTRUCTION]` (`[INTERNAL_API_METHOD]`), já com frases de setup. Se o cliente tiver gíria própria, `[REDACTED_INSTRUCTION]` no mesmo formato.
